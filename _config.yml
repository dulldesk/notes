title: notes
baseurl: "/notes" # the subpath of your site, e.g. /blog
collections:
  notes:
    output: true
    permalink: /:name/

defaults:
  - scope:
      path: "_notes"
    values:
      layout: "note"

sass:
  style: compressed

exclude: ["README.md"]
highlighter: rouge
